{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace @frontend/client dev\" \"yarn workspace @frontend/admin dev\"",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
